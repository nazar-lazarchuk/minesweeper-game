import { FC } from 'react';
import { Button } from './components/Button';

const App: FC = () => {
  return (
    <div>
      <Button>
        Let's play!
      </Button>
    </div>
  );
};

export default App;
